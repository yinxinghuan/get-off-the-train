# Crazy Games review screenshots — Get Off the Train

Captured from the guest build at devicePixelRatio 1.

- `before-*` is the default branch (`master`) Crazy Games build.
- `after-*` is `cursor/cg-train-polish-2674`.

Each size has a start frame, one tutorial step, a mid-car frame, a MADE IT card, and a MISSED IT card. 800×450 has start, made, and missed.

The 907 made card is a keyboard clear of car 01 (28s spare on a 32s clock). `after-907-missed` is a brand-new save's first miss. `after-907-missed-seeded` is a save where grip costs 90 and the other two upgrades cost 40. `after-907-car03-made` and `after-907-car05-made` are keyboard clears of those cars. `after-907-upgrades-close` is the upgrade row on the seeded miss, with the lock only on the unaffordable grip button.
